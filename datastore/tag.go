package datastore

/*
TODO: attempt to create generic struct tag that maps to underlying datastore struct tag
Would be interesting to see if a `store` struct tag could be used that could be converted to the underlying
datastore tag when attempting to encode/decode the struct. Most likely messy and slow but would be interesting.
 */
